services:
  svelte-dashboard:
    build: .
    container_name: ha-svelte-dashboard
    ports:
      - "8080:80"
    environment:
      - HA_URL=http://192.168.1.222:8123
      - HA_TOKEN=${HA_TOKEN}
    restart: unless-stopped